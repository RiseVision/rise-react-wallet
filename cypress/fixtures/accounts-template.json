{
  // rename to accounts.json
  // positions are significant (secret[0].id === storedAccounts[0].id)
  "secrets": [
    {
      "id": "FILL_IN",
      "mnemonic": "FILL_IN",
      "passphrase": "FILL_IN"
      // Account requirements:
      // - already voted delegate
      // - at least one transaction
      // - last transaction is an incoming one
    },
    {
      "id": "FILL_IN",
      "mnemonic": "FILL_IN"
      // Account requirements:
      // - no passphrase set
    }
  ],
  "storedAccounts": [
    {
      "id": "FILL_IN",
      "publicKey": "FILL_IN",
      "readOnly": false,
      "fiatCurrency": "USD",
      "name": null,
      "pinned": false
    },
    {
      "id": "FILL_IN",
      "publicKey": "FILL_IN",
      "readOnly": false,
      "fiatCurrency": "USD",
      "name": null,
      "pinned": false
    }
  ]
}
